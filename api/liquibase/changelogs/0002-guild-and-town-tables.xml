<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                       http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.4.xsd">

    <changeSet id="2" author="guilds_app">
        <comment>Add guild and town tables</comment>
        
        <!-- Create town table -->
        <createTable tableName="towns" schemaName="guild_app">
            <column name="id" type="uuid" defaultValueComputed="gen_random_uuid()">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="name" type="varchar(255)">
                <constraints nullable="false" unique="true"/>
            </column>
            <column name="description" type="text">
                <constraints nullable="false"/>
            </column>
            <column name="region" type="varchar(255)">
                <constraints nullable="false"/>
            </column>
            <column name="properties" type="jsonb" defaultValue="'{}'::jsonb">
                <constraints nullable="false"/>
            </column>
            <column name="created_at" type="timestamp with time zone" defaultValueComputed="NOW()">
                <constraints nullable="false"/>
            </column>
            <column name="updated_at" type="timestamp with time zone" defaultValueComputed="NOW()">
                <constraints nullable="false"/>
            </column>
        </createTable>
        
        <!-- Create guild table -->
        <createTable tableName="guilds" schemaName="guild_app">
            <column name="id" type="uuid" defaultValueComputed="gen_random_uuid()">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="name" type="varchar(255)">
                <constraints nullable="false" unique="true"/>
            </column>
            <column name="description" type="text">
                <constraints nullable="false"/>
            </column>
            <column name="guild_master_id" type="uuid">
                <constraints nullable="true" 
                             foreignKeyName="fk_guild_master"
                             references="guild_app.characters(id)"/>
            </column>
            <column name="town_id" type="uuid">
                <constraints nullable="true" 
                             foreignKeyName="fk_guild_town"
                             references="guild_app.towns(id)"/>
            </column>
            <column name="reward_divider_percentage" type="integer" defaultValue="80">
                <constraints nullable="false" checkConstraint="reward_divider_percentage BETWEEN 0 AND 100"/>
            </column>
            <column name="stash_reward" type="integer" defaultValue="0">
                <constraints nullable="false" checkConstraint="stash_reward >= 0"/>
            </column>
            <column name="quests_completed" type="integer" defaultValue="0">
                <constraints nullable="false" checkConstraint="quests_completed >= 0"/>
            </column>
            <column name="created_at" type="timestamp with time zone" defaultValueComputed="NOW()">
                <constraints nullable="false"/>
            </column>
            <column name="updated_at" type="timestamp with time zone" defaultValueComputed="NOW()">
                <constraints nullable="false"/>
            </column>
        </createTable>
        
        <!-- Create guild membership table -->
        <createTable tableName="character_guilds" schemaName="guild_app">
            <column name="character_id" type="uuid">
                <constraints nullable="false" 
                             foreignKeyName="fk_character_guilds_character"
                             references="guild_app.characters(id)"
                             deleteCascade="true"/>
            </column>
            <column name="guild_id" type="uuid">
                <constraints nullable="false" 
                             foreignKeyName="fk_character_guilds_guild"
                             references="guild_app.guilds(id)"
                             deleteCascade="true"/>
            </column>
            <column name="is_active" type="boolean" defaultValueBoolean="false">
                <constraints nullable="false"/>
            </column>
            <column name="joined_at" type="timestamp with time zone" defaultValueComputed="NOW()">
                <constraints nullable="false"/>
            </column>
        </createTable>
        
        <addPrimaryKey tableName="character_guilds" 
                      columnNames="character_id, guild_id" 
                      constraintName="pk_character_guilds"
                      schemaName="guild_app"/>
        
        <!-- Create AI teammate table -->
        <createTable tableName="ai_teammates" schemaName="guild_app">
            <column name="guild_id" type="uuid">
                <constraints primaryKey="true" nullable="false" 
                             foreignKeyName="fk_ai_teammate_guild"
                             references="guild_app.guilds(id)"
                             deleteCascade="true"/>
            </column>
            <column name="character_id" type="uuid">
                <constraints nullable="false" 
                             foreignKeyName="fk_ai_teammate_character"
                             references="guild_app.characters(id)"
                             deleteCascade="true"/>
            </column>
            <column name="created_at" type="timestamp with time zone" defaultValueComputed="NOW()">
                <constraints nullable="false"/>
            </column>
        </createTable>
        
        <!-- Add guild-related fields to characters table -->
        <addColumn tableName="characters" schemaName="guild_app">
            <column name="active_guild_id" type="uuid">
                <constraints nullable="true"
                             foreignKeyName="fk_character_active_guild"
                             references="guild_app.guilds(id)"/>
            </column>
        </addColumn>
        
        <!-- Add guild-related fields to quests table -->
        <addColumn tableName="quests" schemaName="guild_app">
            <column name="assigned_guild_id" type="uuid">
                <constraints nullable="true"
                             foreignKeyName="fk_quest_assigned_guild"
                             references="guild_app.guilds(id)"/>
            </column>
            <column name="contact_character_id" type="uuid">
                <constraints nullable="true"
                             foreignKeyName="fk_quest_contact_character"
                             references="guild_app.characters(id)"/>
            </column>
        </addColumn>
        
        <!-- Create indexes -->
        <createIndex tableName="character_guilds" 
                    indexName="idx_character_guilds_character_id" 
                    schemaName="guild_app">
            <column name="character_id"/>
        </createIndex>
        
        <createIndex tableName="character_guilds" 
                    indexName="idx_character_guilds_guild_id" 
                    schemaName="guild_app">
            <column name="guild_id"/>
        </createIndex>
        
        <createIndex tableName="character_guilds" 
                    indexName="idx_character_guilds_is_active" 
                    schemaName="guild_app">
            <column name="is_active"/>
        </createIndex>
        
        <createIndex tableName="characters" 
                    indexName="idx_characters_active_guild_id" 
                    schemaName="guild_app">
            <column name="active_guild_id"/>
        </createIndex>
        
        <createIndex tableName="guilds" 
                    indexName="idx_guilds_guild_master_id" 
                    schemaName="guild_app">
            <column name="guild_master_id"/>
        </createIndex>
        
        <createIndex tableName="guilds" 
                    indexName="idx_guilds_town_id" 
                    schemaName="guild_app">
            <column name="town_id"/>
        </createIndex>
        
        <createIndex tableName="quests" 
                    indexName="idx_quests_assigned_guild_id" 
                    schemaName="guild_app">
            <column name="assigned_guild_id"/>
        </createIndex>
        
        <createIndex tableName="quests" 
                    indexName="idx_quests_contact_character_id" 
                    schemaName="guild_app">
            <column name="contact_character_id"/>
        </createIndex>
        
        <createIndex tableName="towns" 
                    indexName="idx_towns_region" 
                    schemaName="guild_app">
            <column name="region"/>
        </createIndex>
        
        <!-- Create PostgreSQL functions -->
        <sql splitStatements="false">
            -- Create helper functions
            CREATE OR REPLACE FUNCTION guild_app.find_guilds_by_skills(skill_ids UUID[])
            RETURNS TABLE (
                guild_id UUID,
                guild_name VARCHAR(255),
                description TEXT,
                matching_skills_count BIGINT,
                total_skills BIGINT,
                quests_completed INTEGER
            ) AS $$
            BEGIN
                RETURN QUERY
                WITH guild_skills AS (
                    SELECT 
                        g.id as guild_id,
                        COUNT(DISTINCT cs.skill_id) FILTER (WHERE cs.skill_id = ANY(skill_ids)) as matching_count,
                        COUNT(DISTINCT cs.skill_id) as total_count
                    FROM 
                        guild_app.guilds g
                    JOIN 
                        guild_app.character_guilds cg ON g.id = cg.guild_id
                    JOIN 
                        guild_app.character_skills cs ON cg.character_id = cs.character_id
                    GROUP BY 
                        g.id
                )
                SELECT 
                    g.id as guild_id,
                    g.name as guild_name,
                    g.description,
                    COALESCE(gs.matching_count, 0) as matching_skills_count,
                    COALESCE(gs.total_count, 0) as total_skills,
                    g.quests_completed
                FROM 
                    guild_app.guilds g
                LEFT JOIN 
                    guild_skills gs ON g.id = gs.guild_id
                WHERE 
                    COALESCE(gs.matching_count, 0) > 0
                ORDER BY 
                    gs.matching_count DESC,
                    g.quests_completed DESC;
            END;
            $$ LANGUAGE plpgsql;

            CREATE OR REPLACE FUNCTION guild_app.find_guilds_in_town(p_town_id UUID)
            RETURNS TABLE (
                guild_id UUID,
                guild_name VARCHAR(255),
                description TEXT,
                guild_master_name VARCHAR(255),
                member_count BIGINT,
                quests_completed INTEGER
            ) AS $$
            BEGIN
                RETURN QUERY
                SELECT 
                    g.id as guild_id,
                    g.name as guild_name,
                    g.description,
                    c.name as guild_master_name,
                    COUNT(DISTINCT cg.character_id) as member_count,
                    g.quests_completed
                FROM 
                    guild_app.guilds g
                LEFT JOIN 
                    guild_app.characters c ON g.guild_master_id = c.id
                LEFT JOIN 
                    guild_app.character_guilds cg ON g.id = cg.guild_id
                WHERE 
                    g.town_id = p_town_id
                GROUP BY 
                    g.id, c.name
                ORDER BY 
                    g.name ASC;
            END;
            $$ LANGUAGE plpgsql;

            -- Create trigger to ensure character can only be in 3 guilds max
            CREATE OR REPLACE FUNCTION guild_app.check_guild_limit()
            RETURNS TRIGGER AS $$
            DECLARE
                guild_count INTEGER;
            BEGIN
                SELECT COUNT(*) INTO guild_count
                FROM guild_app.character_guilds
                WHERE character_id = NEW.character_id;
                
                IF guild_count >= 3 AND TG_OP = 'INSERT' THEN
                    RAISE EXCEPTION 'Character can only be a member of up to 3 guilds';
                END IF;
                
                RETURN NEW;
            END;
            $$ LANGUAGE plpgsql;

            CREATE TRIGGER check_guild_limit_trigger
            BEFORE INSERT ON guild_app.character_guilds
            FOR EACH ROW
            EXECUTE FUNCTION guild_app.check_guild_limit();

            -- Create trigger to add guild master as member when creating a guild
            CREATE OR REPLACE FUNCTION guild_app.add_guild_master_as_member()
            RETURNS TRIGGER AS $$
            BEGIN
                IF NEW.guild_master_id IS NOT NULL THEN
                    INSERT INTO guild_app.character_guilds (character_id, guild_id, is_active)
                    VALUES (NEW.guild_master_id, NEW.id, TRUE)
                    ON CONFLICT (character_id, guild_id) DO UPDATE
                    SET is_active = TRUE;
                    
                    -- Update character's active guild
                    UPDATE guild_app.characters
                    SET active_guild_id = NEW.id
                    WHERE id = NEW.guild_master_id;
                END IF;
                
                RETURN NEW;
            END;
            $$ LANGUAGE plpgsql;

            CREATE TRIGGER add_guild_master_trigger
            AFTER INSERT ON guild_app.guilds
            FOR EACH ROW
            EXECUTE FUNCTION guild_app.add_guild_master_as_member();

            -- Create trigger to ensure only one guild is active per character
            CREATE OR REPLACE FUNCTION guild_app.ensure_one_active_guild()
            RETURNS TRIGGER AS $$
            BEGIN
                IF NEW.is_active = TRUE THEN
                    -- Set all other guild memberships to inactive
                    UPDATE guild_app.character_guilds
                    SET is_active = FALSE
                    WHERE character_id = NEW.character_id
                    AND guild_id != NEW.guild_id;
                    
                    -- Update the character's active guild
                    UPDATE guild_app.characters
                    SET active_guild_id = NEW.guild_id
                    WHERE id = NEW.character_id;
                END IF;
                
                RETURN NEW;
            END;
            $$ LANGUAGE plpgsql;

            CREATE TRIGGER ensure_one_active_guild_trigger
            AFTER INSERT OR UPDATE ON guild_app.character_guilds
            FOR EACH ROW
            WHEN (NEW.is_active = TRUE)
            EXECUTE FUNCTION guild_app.ensure_one_active_guild();

            -- Create trigger to update timestamps
            CREATE OR REPLACE FUNCTION guild_app.update_modified_column()
            RETURNS TRIGGER AS $$
            BEGIN
                NEW.updated_at = NOW();
                RETURN NEW;
            END;
            $$ LANGUAGE plpgsql;

            CREATE TRIGGER update_guilds_timestamp
            BEFORE UPDATE ON guild_app.guilds
            FOR EACH ROW
            EXECUTE FUNCTION guild_app.update_modified_column();

            CREATE TRIGGER update_towns_timestamp
            BEFORE UPDATE ON guild_app.towns
            FOR EACH ROW
            EXECUTE FUNCTION guild_app.update_modified_column();
        </sql>
        
        <!-- Insert sample towns -->
        <loadData tableName="towns" 
                 schemaName="guild_app" 
                 file="changelogs/data/sample_towns.csv"
                 separator="," 
                 quotchar="'"
                 encoding="UTF-8">
            <column name="name" type="STRING"/>
            <column name="description" type="STRING"/>
            <column name="region" type="STRING"/>
            <column name="properties" type="STRING"/>
        </loadData>
    </changeSet>

</databaseChangeLog> 